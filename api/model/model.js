module.exports = { 

    suma: function(x, y) {
        return x + y;
    },

    dividir: function(x, y) {
        return x / y;
    },

    mult: function(x, y) {
        return x * y;
    },

    resta: function(x, y) {
        return x - y;
    }
}
